# Medusa Backend Environment Variables
# ⚠️ SECURITY: Copy this file to .env and fill in real values
# ⚠️ NEVER commit .env file to git - it should be in .gitignore

# Server Configuration
PORT=3001
NODE_ENV=production

# Database Configuration (REQUIRED)
# Get your connection string from MongoDB Atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# 🔐 FLAG SECURITY - CRITICAL: Store real flags in .env, not in code!
# Replace these placeholder values with your actual CTF flags
ROUND1_FLAG=MEDUSA{your_round1_flag_here}
ROUND2_ANDROID_FLAG=MEDUSA{your_android_exploitation_flag_here}
ROUND2_PWN_USER_FLAG=HashX{your_pwn_user_flag_here}
ROUND2_PWN_ROOT_FLAG=HashX{your_pwn_root_flag_here}

# JWT Secret for authentication (REQUIRED)
# Generate strong secret with: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin Panel Credentials (REQUIRED)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-to-strong-password

# Admin Route Path (Security through obscurity)
# Generate random path with: openssl rand -hex 32
ADMIN_ROUTE_PATH=9c8f7e3a2b1d4c5e6f7a8b9c0d1e2f3a

# Round API Keys (REQUIRED for team authentication)
# Change these to your actual API keys for each round
ROUND1_API_KEY=MEDUSA_R1_2025
ROUND2_API_KEY=MEDUSA_R2_2025

# Google Cloud Storage Configuration (REQUIRED for payment uploads)
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id
GOOGLE_CLOUD_STORAGE_BUCKET=your-bucket-name
GOOGLE_CLOUD_KEY_FILE=gcp-service-account.json

# CORS Configuration (Optional)
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://medusa.ecsc-uok.com,https://www.medusa.ecsc-uok.com
